import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#176B87",
    },
    secondary: {
      main: "#04364A",
    },
  },
});

export default theme;
